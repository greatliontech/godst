# DST audit hardening and optimization sequence

**Lands:** before new DST features.

## Audit scope

Full DST changeset audit: `git diff go1.26.4..HEAD`.

Authoritative contract: `docs/dst/design.md`.

Sequence rule: items are ordered **bottom-up** by dependency layer, not by severity. Fix lower substrate issues before relying on higher-level API, package-seam, and feature work.

## Bottom-up sequence (first audit)

| Seq | Severity | Finding | Hardening / optimization action |
|-----|----------|---------|----------------------------------|
| BU-01 | H | Resolved: nested or overlapping `simulation.Run` could corrupt global DST state — `run` mutated `GOMAXPROCS`, async preempt, strategy, sim identity, memory limit, and `dstSeed` before `synctest.Run` rejected nested bubbles. | `enterSimulation` now panics on in-bubble calls (`synctest.IsInBubble`) and on the `runActive` CAS before any state mutation (`src/testing/simulation/simulation.go:259`). Regressions `DSTRunNestedGuard`/`DSTRunOverlapGuard` prove the outer run's DST state survives an inner rejected call. |
| BU-02 | H | Resolved (explicit paths): SUT code could disable the scheduler seam by changing `GOMAXPROCS` inside `Run`. Public `runtime.GOMAXPROCS`/`SetDefaultGOMAXPROCS` are now ignored while `dstActive` (`src/runtime/debug.go:81`, `:122`), tested by same-seed replay after an attempted in-run change. **Residue:** the sysmon auto-updater path was still open — tracked as A2-03 below. | Gated explicit setters; sysmon auto-update gating tracked as A2-03. |
| BU-03 | H | Resolved: run-end `sync.Pool` reap happened after `dstDeactivate`, so pooled finalizer/cleanup objects could be discovered by ordinary async workers without a bubble. | Reap folded into the in-bubble run-end fixpoint: `dstStopGCDrain` loops GC+drain with two quiet pool generations before deactivation (`src/runtime/synctest.go:466`). Tests `TestDSTPoolReapedAcrossRuns`, `TestDSTPooledFinalizerRunEndInBubble`, `TestDSTPooledCleanupRunEndInBubble`. |
| BU-04 | M | Resolved (recording): finalizer/cleanup panics on the DST drain are now recorded by `Explore` (`dstExploreRecordUncaughtPanic` no longer excludes the drain; `src/runtime/dst_explore.go:668`). Tests `TestExploreReportsDrainCallbackPanic*`. **Residue:** post-panic drainless-run fatals and the Goexit sibling path — tracked as A2-01/A2-02 below. | Drain-death residue tracked as A2-01/A2-02. |
| BU-05 | H | Resolved: scheduled replay identity could collide across repeated `Explore` runs (stale root `dstSeq`). | `dstClearSchedState` clears pooled gs (`src/runtime/proc.go:5431`) and the root at bubble exit (`src/runtime/synctest.go:204`). Regression `TestExploreResetsScheduledIdentityAcrossRuns` plus `assertUniqueEnabledSeqs`. |
| BU-06 | M | Resolved: RWMutex DST HB modeling over-ordered failed public operations (embedded writer-mutex HB on failed public `TryLock`). | `*NoDstHB` internal paths; public HB only on successful public operations (`src/internal/sync/mutex.go:62`, `src/sync/rwmutex.go:168`). Regression `TestExploreRWMutexFailedTryLockDoesNotRecordHB`. |
| BU-07 | M | Resolved: DPOR's offline HB relation missed unbuffered-rendezvous, close→closed-receive, and RWMutex sem edges. | Edges added (`src/runtime/chan.go:994`, `:458`, `:608`, `:638`, `src/runtime/select.go:543`, `src/sync/dst_on.go:25`). Tests `TestExploreRecordsUnbufferedChannelHB/ChannelCloseHB/RWMutexHB`. Residual missing buffered k→k+C edge (over-exploration only) tracked as A2-13. |
| BU-08 | M | Resolved: the live shared-address filter did not consume sync HB. | `dstApplyLiveSyncEvent` maintains live object clocks for every sync event before buffering (`src/runtime/dst_explore.go:653`); overflow falls back to yield-everything (`dstFilterConservative`). |
| BU-09 | M | Resolved: Level-2 build-isolation contract was stronger than the implementation. `runtime.g` has DST fields unconditionally (`src/runtime/runtime2.go:510`), `dst_explore.go` is compiled without `dst`/`race` build tags (`src/runtime/dst_explore.go:1`), and hook branches are present in normal source paths behind constants (`src/internal/sync/mutex.go:66`, `src/runtime/chan.go:196`, `src/runtime/select.go:265`). If `DST-L2-4` really meant byte-identical non-DST and dst-without-race builds, this changed layout/source artifacts even when behavior was gated. | Amended `DST-L2-4` to the selected build-mode inertness contract and added `TestDSTAccessYieldBuildModeInert` to prove scalar and range access-yield hooks are emitted only under `-tags dst -race`. |
| BU-10 | M | Resolved: `RunWith`/replay API and validation did not match the design. `docs/dst/design.md` described `Options{Strategy: DPOR, Schedule: s}` replay, but public `Options` exposes only `Random`/`PCT` fields (`src/testing/simulation/simulation.go:155`, `src/testing/simulation/simulation.go:182`) and scheduled replay is through `Replay`/internal `run`. Unknown `Strategy` values silently ran Random because only `PCT` was special-cased (`src/testing/simulation/simulation.go:289`). `Depth`/`Steps` also converted to `int32` without bounds checks (`src/testing/simulation/simulation.go:296`). | Amended the design to the landed `Explore`/`Replay` API, made `RunWith` reject unknown strategies, and made positive PCT `Depth`/`Steps` panic before activation if they exceed the runtime scheduler's `int32` field. |
| BU-11 | H | Resolved: typed and packet network APIs bypassed DST and could open real host sockets. The DST fast path was only in string `DialContext` and `Listen` (`src/net/dial.go:529`, `src/net/dial.go:878`). Typed APIs and packet listeners fell through to OS dial/listen paths (`src/net/dial.go:606`, `src/net/dial.go:620`, `src/net/dial.go:634`, `src/net/dial.go:648`, `src/net/dial.go:917`). | Added DST guards to typed TCP/UDP/IP/Unix dial/listen paths and `ListenPacket`; these public entry points now return a deterministic unsupported-network-API error under `simulation.Run` instead of reaching the host OS. Added `TestDSTNetTypedAndPacketAPIsDoNotTouchHost`. **Residue:** the `File*` trio was outside this enumeration — tracked as A2-16. |
| BU-12 | M | Resolved: `Dialer` and `ListenConfig` options were silently ignored on the DST fast path. `DialContext` dropped the `Dialer` before calling `dstDial` (`src/net/dial.go:529`, `src/net/dst.go:291`); `Listen` dropped `ListenConfig` before `dstListen` (`src/net/dial.go:878`, `src/net/dst.go:244`). `LocalAddr`, `Control`, `ControlContext`, MPTCP, and keepalive semantics could silently differ from production. | Passed `Dialer`/`ListenConfig` into the DST shim, honored compatible `Dialer.LocalAddr`, accepted explicit MPTCP disable as ordinary TCP, and rejected explicit raw-fd/MPTCP-enable/keepalive options with deterministic unsupported-option errors. Added option-behavior tests. |
| BU-13 | M | Resolved: DST net errors lost standard error identity. Refused connections and duplicate listen used fresh `errors.New` values (`src/net/dst.go:276`, `src/net/dst.go:323`, `src/net/dst.go:344`), so `errors.Is(err, syscall.ECONNREFUSED)` / `EADDRINUSE` could fail under DST while succeeding in production-shaped code. | Preserved production-shaped error identity by using `syscall.ECONNREFUSED` for refused connects and `syscall.EADDRINUSE` for duplicate listens under the existing `*OpError` wrapper. Added `TestDSTNetErrorIdentity`. **Residue:** established-conn error identity — tracked as A2-18. |
| BU-14 | M | Resolved: direct DNS resolver APIs still used the host resolver inside `Run`. The design rejected DNS through `Dial`/`Listen`, but public `net.Lookup*` / `Resolver` methods were not gated and could do host DNS I/O. | Gated public lookup/resolver APIs under DST with deterministic unsupported DNS/service errors, while preserving literal-IP, numeric-port, and validation fast paths. Added tests that `net.LookupHost("example.com")` and sibling lookup APIs do not reach the host resolver inside `Run`. |
| BU-15 | M | Process identity is only partially virtualized despite broad contract language. `os.Getgroups` still returns host groups (`src/os/proc.go:80`), and `os/user.Lookup`, `LookupId`, `LookupGroup`, `LookupGroupId`, and `User.GroupIds` fall through to host user/group databases except for the current simulated user fast path (`src/os/user/lookup.go:46`, `src/os/user/lookup.go:64`, `src/os/user/lookup.go:75`). | Simulate a fixed group list and minimal user/group database, or narrow the spec/package docs to current-user-only and add tests for the chosen boundary. |
| BU-16 | M | The crypto/rand outside-run test can pass even if outside-run bytes are still deterministic. The test program only checks that two outside reads differ within one process (`src/runtime/testdata/testprog/dst.go:485`), and the parent test compares only the printed booleans across processes (`src/runtime/dst_test.go:875`). A bad mutation that leaves `dstActive` true after `Run` but advances the deterministic stream can still print `realdiffers=true`. | Assert `runtime.dstActive` is false after `Run` in the test program and/or print/compare outside-run bytes across separate processes so deterministic outside-run output is caught. |
| BU-17 | L | `httptrace`/nettrace connect callbacks are skipped by the DST dial fast path. `DialContext` returns from `dstDial` before trace extraction (`src/net/dial.go:529`), so `ConnectStart`/`ConnectDone` are not fired around simulated connects. | Move the DST fast path after trace extraction or explicitly fire `ConnectStart`/`ConnectDone` around `dstDial`. Add an `httptrace` regression. |
| BU-18 | L | Resolved: authoritative docs/spec drift on GC drain timing — the "Decision for the build" paragraph claimed Chunk B's drain runs per-GC while the implementation (and the "As built" paragraph) drain at quiescence. | Amended the decision paragraph to the as-built contract: per-cycle *discovery* determinism with callback *execution* at quiescence (separate axes). |
| BU-19 | L | Resolved: package docs described the GC trigger byte as accounting noise after the implementation moved the trigger to per-object `dstHeapAlloc`. | `Run` doc now states the per-object trigger and per-cycle discovery are contractual with only byte-level MemStats as noise; the `DSTGCFinDiscovery` testprog comment now cites the per-cycle contract and its enforcing test instead of denying it. |
| BU-20 | L | Resolved: roadmap status was stale for Level 2 (listed as pending design while `Explore`/`ExploreWith`/`Replay` shipped). | Level 2 moved to the Landed roadmap section; the Level-2 design section header now reads "implemented and validated (D1–D5)". |
| BU-21 | Opt | Access filtering scans all recorded access entries on every auto-instrumented access (`src/runtime/dst_explore.go:434`), an O(A^2) path for heap-heavy SUTs even when most accesses are private or non-overlapping. | Replace the full scan with interval/page buckets or another range index for overlap candidates. Keep conservative overflow behavior. |
| BU-22 | Opt | Forced-access replay checks scan every forced triple on every access (`src/runtime/dst_explore.go:409`), O(A*F) as promotion grows. | Sort installed forces and binary-search, or shard by `seq` / `(seq,count)` for constant or logarithmic lookup. |

## Bottom-up sequence (second audit, 2026-06)

Fresh full audit of `git diff go1.26.4..HEAD` after the first-audit fixes landed. Same sequence rule.

| Seq | Severity | Finding | Hardening / optimization action |
|-----|----------|---------|----------------------------------|
| A2-01 | H | The GC drain is woken without checking where it is parked: `dstDrainAtQuiescence` (`src/runtime/synctest.go:441`) and the exit wake in `dstStopGCDrain` `goready` the drain after only `finPending()/cleanupPending()`. (a) A finalizer blocking on a bubble channel (endorsed by design D4) leaves the drain parked in `chanrecv`; a pending-work quiescence then goreadies it there → spurious receive and `fatal error: runtime: sudog with non-nil elem` (reproduced). (b) A callback reaching `runtime.Goexit` (e.g. `t.FailNow`) kills the drain but only the panic path nils `bubble.gcDrain` (`src/runtime/dst_explore.go:678`); the next wake hits a dead g → `fatal error: bad g->status in ready` (reproduced). | Wake only when the drain is parked at `waitReasonSynctestGCDrain` (skipping is sound: the drain loops until empty on resume); clear `bubble.gcDrain` on any drain death, not only panic. Regressions: blocking finalizer across a pending-work quiescence; Goexit in a callback. |
| A2-02 | M | After a recorded drain-callback panic (BU-04 path) the run continues with no drain: later trigger-GCs queue callbacks nothing drains, the run-end pool reap is skipped, and at `dstDeactivate` the queued callbacks run unbubbled — a bubble-channel-touching one fatals after `Failure.Panic` was already reported. A drain panic also abandons the rest of the detached `finBlock` chain (`src/runtime/mfinal.go:300`): those finalizers never run and pin their arguments. | When the drain has died, discard the queued in-run finalizer/cleanup chains deterministically at teardown so nothing leaks to bubble-less workers; account the discarded chain so metrics do not skew. Regression: drain panic + later channel-touching finalizer → process survives, failure reported. |
| A2-03 | M | BU-02 residue: `sysmonUpdateGOMAXPROCS` (`src/runtime/proc.go:7201`) checks only `customGOMAXPROCS`. In a process whose GOMAXPROCS auto-computed to 1, a mid-run cgroup-quota/affinity change STWs and `procresize`s to >1; the seam gate (`dstActive() && gomaxprocs == 1`, `src/runtime/proc.go:3499`) silently disengages. The existing regression runs with `GOMAXPROCS=4` env, which sets the custom flag, so the auto configuration is never exercised. | Gate the sysmon auto-updater on `!dstActive()` (mirroring forcegc), or force the custom flag for the duration of the pin. |
| A2-04 | M | `firstSystemG` (`src/runtime/proc.go:7851`) treats any `gp.bubble != nil` candidate as simulation-owned; a concurrent plain `testing/synctest` bubble (parallel test) consumes `dstSchedRand` draws and interleaves into the seeded schedule, so the same seed replayed in isolation produces a different schedule — against the bubble-isolation contract. | Treat only the active simulation's bubble as simulation-owned; foreign-bubble goroutines take the RNG-free system path (or `Run` rejects/serializes against foreign bubbles). Regression with a concurrent synctest bubble. |
| A2-05 | M | `dstHeapAlloc.Add` fires for every allocation under `dstActive()` (`src/runtime/malloc.go:1225`), bubble or not; any mid-run non-bubble allocation (e.g. a leaked goroutine waking on a real timer) moves the cycle-boundary crossing, so NumGC and which cycle discovers a finalizer vary run-to-run — contradicting the "GC discovery (unconditional)" contract. | Count `dstHeapAlloc` only for bubble allocations. Regression: non-bubble allocating goroutine alive across a run. |
| A2-06 | M | `synctestRun` re-roots `bubble.main.dstrand` to the same root the activation gave the caller (`src/runtime/synctest.go:220` = `dstBubbleRoot(seed)`, `src/runtime/dst.go:115`), so bubble.main replays the caller's first draws (which seeded bubble.main and `bubble.gcDrain`): the second child goroutine spawned by the SUT main shares a bit-identical RNG stream with the finalizer drain (same map seeds, same crypto/rand bytes). `dstSchedRoot` is salted against exactly this class; the bubble re-root is not. | Salt the bubble re-root (and/or the drain seed) independently of the activation root. Stream-uniqueness regression across main's first children and the drain. |
| A2-07 | L | The post-run restore `runtime.GOMAXPROCS(oldProcs)` (`src/testing/simulation/simulation.go`) sets `customGOMAXPROCS` permanently; a process that was in container-aware auto mode before its first `Run` stops tracking cgroup changes for the rest of the process. | Restore auto mode after the run when the process was auto before it. |
| A2-08 | L | Simulated identity gates on `dstSimEnvSet` alone, which is set before `dstActivate` and cleared after `dstDeactivate`; non-bubble goroutines can observe simulated identity outside the active-run window (single-P interleaving only). | Narrow the window or document it as part of the identity boundary. |
| A2-09 | L | Under `-tags dst`, `startupRand` is no longer scrubbed (`src/runtime/rand.go:682`): OS entropy bytes stay readable in memory in dst-tagged builds. | Scrub as upstream does regardless of the dst seed path. |
| A2-10 | M | `sync/atomic` operations (and `len(ch)`/`cap(ch)`) are not exploration transitions: a two-goroutine CAS-winner SUT explores one of two outcome classes under both Exhaustive and DPOR yet reports `Exhausted=true`. Neither the design's "Completeness boundary" nor the `Explore` API docs name the exclusion — the prog#257 lesson replayed for atomics. (TSan still models atomics as sync, so no false races; the missed direction is assertion failures.) | Name the atomics/len-cap boundary in design.md "Completeness boundary" and the `Explore` API doc. Hooking atomics as decision points (a `dstSyncAcquire` analog at TSan's atomic entry points) is a candidate feature — tracked here until scheduled. |
| A2-11 | M | The auto-instrumented DST-L2-3 enforcement (`TestDSTExploreAutoInstrument`) compares DPOR vs Exhaustive with both sides under the same live filter + promotion; a filter defect that drops an access class drops it from both sides and the equivalence still passes. Only the manual-hook brain is guarded by the unfiltered 290-program sweep. | Harden: known-outcome-set tests across promoted/filtered shapes, or an unfiltered cross-check leg for the auto-instrumented path. |
| A2-12 | L | A SUT panic masks the schedule-prefix-divergence panic (`src/testing/simulation/explore.go:372` fires only when `out.panic == ""`), recording a `Failure` whose schedule did not actually replay. | Report the divergence alongside the SUT panic instead of suppressing it. |
| A2-13 | L | Unbuffered rendezvous and close share sync-object key `(c, aux=0)` (`src/runtime/chan.go:994` vs `:458`), an extra HB edge TSan does not model (rendezvous participant → later closed-receiver); the buffered k'th-receive→(k+C)'th-send slot-reuse edge is missing when the sender never blocks (over-exploration only, BU-07 residual). | Split the aux key for close vs rendezvous; add the slot-reuse edge. |
| A2-14 | L | Sync-HB event buffer overflow drops events silently (`src/runtime/dst_explore.go:654`), unlike `dstEdgeOverflow`. Sound (live clocks are applied before buffering; missing offline edges only enlarge the concurrent set) but the asymmetry is undocumented. | Document the conservativeness argument at the buffer declaration. |
| A2-15 | L | `Replay` reports `raced` without verifying the replayed race is the same race (no signature comparison); a different new race counts as confirmation. | Compare the replayed race identity (or document the boundary on `Replay`). |
| A2-16 | H | `net.FileConn`/`FileListener`/`FilePacketConn` (`src/net/file.go`) have no DST gate — the only `net` entry points returning live conns/listeners that bypass the shim. Inside `simulation.Run`, an inherited socket fd yields a fully real, nondeterministic OS listener; even a non-socket file performs host syscalls before failing. Violates the close-out criterion that no unsupported seam falls through to host I/O. | Gate all three under DST with the deterministic unsupported-network-API error; regression. |
| A2-17 | M | `dstListener.Close` (`src/net/dst.go:318`) never drains the queued established conns in `l.accept`: a dialer that already got success blocks durably forever on `Read` → synctest deadlock panic where production TCP RSTs the backlog; a post-close `Accept` can still return a queued conn instead of `net.ErrClosed`. | On Close, drain and close queued conns; `Accept` checks closed state first. Regressions for both. |
| A2-18 | M | Established-conn errors are pipe-shaped: read/write-after-close and write-to-peer-closed return bare `io.ErrClosedPipe`; `errors.Is(err, net.ErrClosed)` is false after close, `ECONNRESET` never holds, wrapped errors carry `Net:"pipe"` without addresses. The spec only records identity for refused-connect/dup-listen. | Wrap DST conns production-shaped: post-close ops → `net.ErrClosed` in `*OpError{Net:"tcp", ...}` with addresses; write-to-peer-closed → `ECONNRESET`. Record the contract in the spec; identity tests. |
| A2-19 | M | No dual-stack wildcard: `Listen("tcp", ":p")` registers IPv4-family-only (`dstHostIP`, `src/net/dst.go:178`), so `Dial("tcp", "[::1]:p")` gets `ECONNREFUSED` where production dual-stack accepts; `Addr()` reports `127.0.0.1:p`. | Model the plain-`"tcp"` wildcard as dual-stack (both family keys), `Addr()` reporting the wildcard form. Regression. |
| A2-20 | M | `GODEBUG=fips140=on` silently breaks crypto/rand determinism inside a run: `crypto/rand` routes through the process-global FIPS DRBG which consumes the seam's bytes only as additional input; `Run` succeeds with replay silently broken. The spec's premise that FIPS "needs cgo/special builds" (design.md crypto section) is factually wrong — FIPS mode is one GODEBUG away in a `-tags dst` build. | Fail loud (panic) in `enterSimulation` when FIPS mode is active; correct the spec sentence. |
| A2-21 | M | `TestWith`'s `propagateGoexit=true` is unpinned (flipping it survives the full `-tags dst` suite) and observably diverges from `testing/synctest.Test` in the grandparent chain-abort case (FailNow on a grandparent T from inside the simulation child: `true` propagates the abort through the whole chain; `false` — synctest.Test's behavior — lets the root test continue), while design.md claims "the same control semantics as testing/synctest.Test". | Pin the chosen semantics with a grandparent chain-abort regression; amend the design wording to enumerate the control semantics including the chain-abort behavior. |
| A2-22 | L | `os.UserHomeDir`/`TempDir`/`Environ` leak host values inside a run, and `UserHomeDir` (host `$HOME`) disagrees with `user.Current().HomeDir` (`/home/sim`) — two identity reads in one run conflict. Spec is silent on the env-derived surface. | Document the env-derived boundary (virtualization belongs to the dst-io feature); record the incoherence until then. |
| A2-23 | L | DST conn deadlines and close semantics are untested: no test calls `Set{Read,Write}Deadline` on a DST conn inside `Run` (only dial-context deadline is covered), nor covers read-after-close/write-to-peer-closed/EOF edges. | Add virtual-time deadline and close-semantics regressions (pairs with A2-17/A2-18). |
| A2-24 | L | `Dial("udp", ...)` under DST returns `UnknownNetworkError("udp")` — misleading for a known-but-unmodeled network; double `Close` on DST conns/listeners returns nil where production returns `net.ErrClosed`; unsupported-API errors are bare strings matchable only by substring. | Align error text and identity with the typed-API "unsupported under deterministic simulation" shape; double-Close returns `net.ErrClosed`. |
| A2-25 | L | The contract-enforcing DST tests are dead in the plausible CI configuration: `-short` skips the sweep/oracle/build-mode tests, and untagged runs skip every `dstBuilt()`-gated test; no in-repo runner records a configuration that executes them. | Record the enforcing test configuration (runner choice is a user call). |
| A2-26 | L | The documented "requires building with -tags dst" panic has no test (only testable from a non-dst build). | Add a non-dst-build testprog regression. |
| A2-27 | nit | The `t.Cleanup`-reentry panic reached via `simulation.Test` says "testing: synctest.Run called during t.Cleanup" — wrong API name on that path. | Name the entry API in the message. |
| A2-28 | M | Mid-run finalizers/cleanups registered by goroutines OUTSIDE the simulation bubble are discovered by sim GCs and run on the sim drain (`dstDrainFinq` drains global `finq` wholesale; only pre-bubble callbacks are deferred). The discovered set then includes foreign objects, and a foreign callback drawing randomness on the drain advances the drain's per-g stream, diverging later sim callback randomness from the alone run. Found by the chunk-3 review; reproduces on base (adjacent). | Either defer mid-run foreign-owned callbacks like pre-bubble ones (ownership = registering goroutine's bubble), or keep the bounded contract (package doc now scopes GC discovery to simulation-allocated objects) and document the drain exposure. |
| A2-29 | M | Local run-queue overflow defeats foreign order-isolation: `runqputslow` spills the HEAD half of the ring (which enumerates first) to the global queue (which enumerates last), so with >~255 simultaneously runnable goroutines foreign ring occupancy shifts when overflow fires and permutes sim candidate order vs the alone run. Reproduces on base (adjacent); design.md Landed bullet now states the bound. | Make the spill order-preserving for sim candidates under DST (or spill foreign entries first), with an overflow-pressure regression. |
| A2-30 | L | The GOMAXPROCS auto-updater can die permanently across a run: a pending update readied by sysmon before `runLocked`'s pin observes `customGOMAXPROCS` under STW and exits (`updateMaxProcsGoroutine` returns, `idle` stays false), so post-run `SetDefaultGOMAXPROCS` restores the flag but auto updates never resume. Narrow race (needs a quota/affinity change in flight at run entry); adjacent (upstream helper semantics). | Gate the helper's bail to re-park instead of exit while a simulation pin is the cause, or re-arm the helper at restore. |
| A2-31 | L | A blocked `Accept` already parked in its select when `Close` runs can receive a queued connection instead of `net.ErrClosed` (the seeded select may pick the non-empty accept case over the just-closed done case; the closed-first check covers only Accept calls made after Close). Production unblocks pending accepts with ErrClosed unconditionally. Pre-existing select shape (adjacent). | Make Close tear down pending accepts deterministically (e.g. drain claims queued conns before the parked Accept can win them, or Accept rechecks done after receiving). |
| A2-32 | nit | Single-family wildcard `Addr()` reports loopback: `Listen("tcp4", ":0").Addr()` is `127.0.0.1:p` where production reports `0.0.0.0:p` (adjacent; the dual-stack wildcard now reports `[::]:p` correctly). | Report the family wildcard form. |

| A2-33 | L | `testing/simulation` is not `-tags dst -race`-clean: `TestExploreWithScheduleBudgetReportsIncomplete` runs an intentionally racy SUT in-process, and the race report escapes the Level-2 oracle's accounting into the `testing` harness ("race detected during execution of test"). The racy-SUT oracle tests avoid this by running in subprocesses. Pre-existing (reproduces at the audit base); surfaced by the first in-process `-race` run of the suite. | Run intentionally racy in-process SUTs in subprocesses, or reset the harness race accounting through the oracle the way the testprog-based oracle tests do. |

## Close-out criteria

- Each sequence item is either fixed with regression coverage, deliberately folded into another tracked issue, or the spec is amended to the chosen contract.
- No package seam that is marked unsupported falls through to host OS/network/user/database I/O inside `simulation.Run`.
- `docs/dst/design.md` and `testing/simulation` package docs agree on landed API and GC timing/discovery contracts.
- Optimization items are handled after correctness items that affect the same code path, unless the optimization is the simplest way to make an invariant enforceable.
